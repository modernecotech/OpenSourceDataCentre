# Config Script Examples

These files are example source artifacts for the browser-based config editor pattern.

The portal should expose real tool configuration with validation, review, GitOps rollout, and audit rather than trying to model every option from every upstream tool as custom UI fields.

The files under `edge/` are used by the `osdc-portal` sample API.
